# policies

Declarative data the kernel enforces: authorization gates, the security floor, DoD
profiles, data semantics, budgets and stopping rules.

Policy is enforced, not remembered. See ../docs/HUMAN_AUTHORIZATION.md and
../docs/DEFINITION_OF_DONE.md.

Empty in Phase 0 — design only.
