# adapters

The only path between agents and the outside world: repository, git, project-management,
runtime and host adapters. Capability detection, redaction and the authorization interlock
live here.

See ../docs/REPOSITORY_ADAPTER.md.

Empty in Phase 0 — design only.
